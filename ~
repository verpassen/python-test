#!/bin/usr
#-*- coding : utf8 -*- 
import random 
import numpy as np 

'''
t = range(0,6)
k=np.zeros((6,6))
for x in t:
    for y in t:
        #print ('the current pos (x,,y) = (%d , %d)' % (x,y))
        val = random.randint(1,36)
        k[x][y]=val
        y +=y 

    x +=x 

print(k)
'這樣寫會有重複出現的數字'
'''
t = range(0,6)

